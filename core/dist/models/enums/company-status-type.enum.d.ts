/**
 * The [[Company]] status type
 */
export declare enum CompanyStatusType {
    /** The [[Company]] is not approved by an admin yet */
    NotApproved = 0,
    /** The [[Company]] has been approved by an admin */
    Approved = 1,
    /** The [[Company]] has been rejected by an admin */
    Rejected = 2,
}
